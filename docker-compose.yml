version: "3.8"
services:
  mysql-db:
    image: mysql:8
    container_name: mysql-db
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: quizdb
    ports:
      - "3306:3306"
    volumes:
      - ./db-init.sql:/docker-entrypoint-initdb.d/db-init.sql

  tomcat-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: regapp
    ports:
      - "8080:8080"
    depends_on:
      - mysql-db
    links:
      - mysql-db:mysql-db
